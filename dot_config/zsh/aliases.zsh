# Aliases

# Suffix aliases — open files by extension
alias -s json='$EDITOR'
alias -s go='$EDITOR'
alias -s rs='$EDITOR'
alias -s py='$EDITOR'
alias -s js='$EDITOR'
alias -s ts='$EDITOR'
alias -s vue='$EDITOR'
alias -s txt=bat
alias -s md=bat
alias -s log=bat
alias -s html="open"
alias -s mov="open"
alias -s png="open"
alias -s mp4="open"
